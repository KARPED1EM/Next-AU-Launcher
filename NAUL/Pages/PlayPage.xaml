<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NAUL.Page_Play"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NAUL"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid ColumnDefinitions="*, 400" Margin="70, 300, 0, 0">

        <StackPanel Grid.Column="0" Orientation="Vertical">
            <TextBlock Text="{x:Bind titleText}" FontSize="70" FontWeight="ExtraBold" />
            <TextBlock Text="{x:Bind descriptionText}" FontSize="16" Margin="20, -8, 0, 0" />
        </StackPanel>

        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0, 0, 130, -30">
            <Button CornerRadius="23" Margin="0, 0, 10, 0" Width="200" Height="55">
                <StackPanel Orientation="Horizontal">
                    <FontIcon Glyph="&#xE768;" FontSize="18"/>
                    <TextBlock Margin="8, 0, 0, 0" Text="启动" FontSize="18" />
                </StackPanel>
            </Button>
            <Button CornerRadius="23" Width="55" Height="55">
                <FontIcon Glyph="&#xE96D;" FontSize="18"/>
            </Button>
        </StackPanel>
        
    </Grid>
</Page>
